import { StyleSheet, Text, View } from "react-native";

export default function () {
  return (
    <View>
      <Text> Loading Quiz from suspense </Text>
    </View>
  );
}

const styles = StyleSheet.create({});
